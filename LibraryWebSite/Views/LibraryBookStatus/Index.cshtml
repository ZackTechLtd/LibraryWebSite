@model LibraryWebSite.Models.LibraryBookStatusViewModel
@{
    ViewBag.Title = "Library Book Status";
}

<div id="table-actions"
     data-editurl="@Url.Action("LibraryBookStatus", "LibraryBookStatus", new { area = "" })"
     data-deleteurl="@Url.Action("DeleteLibraryBookStatus", "LibraryBookStatus", new { area = "" })"
     data-searchcolumns="librarybookstatus">
</div>

<div class="block">
    @Html.AntiForgeryToken()

    <div id="errorMessage" class="row sr-only">
        <div class="form-group">
            <div class="col-md-12 alert alert-danger">
                @Html.ValidationSummary(null, new { id = "validationSummary" })
            </div>
        </div>
    </div>

    <div id="successMessage" class="row sr-only">
        <div class="form-group">
            <div class="col-md-12 alert alert-success">
                Record Deleted <i class="fa fa-check-circle" aria-hidden="true"></i>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="panel panel-primary wl">
            <div class="panel-heading">
                <div class="row">
                    <h4 class="col-xs-10 col-md-10">
                        <i class="fa fa-building fa-lg" aria-hidden="true"></i>&nbsp;&nbsp; Library Book Status
                    </h4>
                    <div class="col-xs-4 col-md-2">
                        <a class="btn btn-success" href="@Url.Action("LibraryBookStatus","LibraryBookStatus", new { area = "" })">Add New <i class="fa fa-plus-circle" aria-hidden="true"></i></a>
                    </div>
                </div>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table id="pagedResultsDataTable" class="table table-striped-utl table-hover-utl" data-url="@Url.Action("GetLibraryBookStatus", "LibraryBookStatus", new { area = "" })">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Date Checked Out</th>
                                <th>Date Returned</th>
                                <th>ISBN</th>
                                <th>Title</th>
                                <th>Copy Number</th>
                                <th>Name</th>
                                <th>Address</th>
                                <th>Modified By</th>
                                <th>Data Modified</th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


